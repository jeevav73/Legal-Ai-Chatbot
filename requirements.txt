# Core ML
torch>=2.3.0
transformers>=4.44.0
accelerate>=0.33.0
bitsandbytes>=0.43.3        # 4-bit quantization for running/training 7B models on modest GPUs
peft>=0.12.0                # LoRA / QLoRA fine-tuning
trl>=0.9.6                  # SFTTrainer for instruction fine-tuning
datasets>=2.20.0

# Embeddings + vector DB (retrieval half of RAG)
sentence-transformers>=3.0.1
chromadb>=0.5.5

# App layer
streamlit>=1.38.0
python-docx>=1.1.2
python-dotenv>=1.0.1
pypdf>=4.3.1
